name: Release

# Placeholder release workflow for NeatWhisky.
# The upstream Whisky release workflow (Discord webhook + Whisky-App Homebrew cask)
# has been removed because it referenced another org's secrets and cask.
# TODO (M12 分发): sign + notarize the app, attach the DMG to the GitHub Release,
# and bump the NeatWhisky Homebrew cask.

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  placeholder:
    name: Release (not yet configured)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "NeatWhisky release pipeline is not configured yet. See M12 in docs/build-plan.md."
